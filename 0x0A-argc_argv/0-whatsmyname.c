#include <stdio.h>

/**
 * main - Program that print its name, followed by a new line
 * @argc: Argument count
 * argv: Argument vector
 *
 * Return: Always 0.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
